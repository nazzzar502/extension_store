import React from "react";
import PropTypes from "prop-types";

const NoPayType = (props) => {
  return <p className="subtitle is-5">Please select the type of payment</p>;
};

// NoPayType.propTypes = {}

export default NoPayType;
